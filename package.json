{
  "name": "streamhub-tv",
  "version": "1.0.0",
  "description": "StreamHub TV — Netflix-style IPTV streaming hub for Fire TV",
  "private": true,
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 3000",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0 --port 3000",
    "start": "serve -s dist -l 3000"
  },
  "dependencies": {
    "hls.js": "^1.5.7",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.1",
    "vite": "^5.4.2",
    "serve": "^14.2.1"
  }
}
